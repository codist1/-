<!--
<% @output_text.each do |post| %>
    <%= post.name %><br>
    <%= post.where %><br>
    <%= post.content %><br>
    <%= post.img %>
<% end %>
-->
<!--
<div class="row marketing">-->
  <!-- 데이터베이스에서 불러와 반복하여 뿌려줄 방송 정보 -->
  <% @output_text.each do |t|%>
<!--  <div class="col-lg-6">
    <h4><%= t.name %></h4>
    <img src= "<%= t.img %>" class="img-thumbnail" width="320">
    <p><%= t.where %></p>
    <span class="star-rating small">
      <i></i>
      <i></i>
      <i></i>
      <i class="on"></i>
      <i></i>
    </span>
    <p id="count">4.2 점 (250 명)</p>
    <p><a class="btn btn-sm btn-success" href="/home/detail/<%=t.id%>" role="button">자세히 보기</a></p>
  </div>
  <!-- 끝 -->
<!--<%end%>-->



<div class="container">
      <!-- Example row of columns -->
      <div class="row marketing">
              <% @output_text.each do |post| %>
        <div class="col-lg-4">
          <h2><%= post.name %></h2>
          <img src="<%= post.img %>" class="img-thumbnail" width="500">
         
          <p><%= post.content %></p>
         <p><a class="btn btn-sm btn-success" href="/home/detail/<%= post.id %>" role="button">자세히 보기</a></p>
          
        </div>
        
      <%end%>
      
      </div>
</div>   
      <%= render :partial =>"home/basic"%>